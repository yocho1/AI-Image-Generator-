[build]
command = "cd backend && pip install -r requirements.txt"

[run]
command = "cd backend && gunicorn -c gunicorn_config.py app:app"

[env]
PORT = "5002"
FLASK_ENV = "production"

[python]
version = "3.11"